.errorTitleText {
  font-size: 28px;   
  font-weight: 600;
  margin-bottom: 25px;
  color: var(--text-color);
  text-align: center;
}

.errorDescriptionText {
  font-size: 16px;    
  padding: 12px;
  width: 100%;
  margin-bottom: 20px;
  color: var(--text-color);
  text-align: center;
}
